---

- name: Install Python
  homebrew:
    name: "{{ item }}"
    update_homebrew: yes
    upgrade_all: yes
    state: unlinked
  with_items:
    - python

- name: Install global Python packages
  pip:
    name: "{{ item }}"
  with_items:
    - virtualenv